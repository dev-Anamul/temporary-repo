/**
 * @param {string[]} garbage
 * @param {number[]} travel
 * @return {number}
 */
var garbageCollection = function(garbage, travel) {

    let glassCount =0;
    let paperCount =0;
    let metalCount =0;

    for(let i = garbage.length -1;i>=0;i--){
        
    }
    
};