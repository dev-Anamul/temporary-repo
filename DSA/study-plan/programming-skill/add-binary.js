/**
 * @param {string} a
 * @param {string} b
 * @return {string}
 */
var addBinary = function (a, b) {
  if (a.length > b.length) {
    for(let i =a.length -1;i<a.length-1;i++){


    }
  } else {
  }
};
