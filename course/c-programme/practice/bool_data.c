#include <stdio.h>
#include <stdbool.h>

void main()
{

    bool is_logged_in = false;

    if (is_logged_in)
    {
        printf("User Logged In\n");
    }
    else
    {
        printf("User Logged Out\n");
    }
}