const addAsset = require('./add-asset');
const updateAsset = require('./update-asset');
const deleteAsset = require('./delete-asset');
const findAsset = require('./find-asset');
const findAssets = require('./find-assets');

module.exports = {
    addAsset,
    updateAsset,
    deleteAsset,
    findAsset,
    findAssets,
};
